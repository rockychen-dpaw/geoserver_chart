{{- if and (hasKey $.Values "gwcManage") (hasKey $.Values.gwcManage "imagepull") }}
{{- $data := dict "auths" (dict ($.Values.gwcManage.imagepull.domain | default "ghcr.io")  (dict "username" ($.Values.gwcManage.imagepull.username ) "password" ($.Values.gwcManage.imagepull.password ) )) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Release.Name }}-geoserver-gwcmanage-secret-imagepull
  namespace: {{ $.Release.Namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ toJson $data | b64enc }}
{{- end }}
